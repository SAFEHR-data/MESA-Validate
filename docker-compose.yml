services:
  mesa-validate:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./predictions:/app/predictions
      - ./sessions:/app/sessions
      - ./schemas.yaml:/app/schemas.yaml:ro
    restart: unless-stopped
